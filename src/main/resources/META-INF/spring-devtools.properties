# Pull Spring Data JPA into the restart classloader so repository interfaces
# (e.g. UserRepository extending JpaRepository) load in the same classloader
# and JpaRepository is found (avoids NoClassDefFoundError with devtools).
restart.include.springdatajpa=spring-data-jpa-[\\w\\d-\\.]+\\.jar
